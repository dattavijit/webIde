sap.ui.define([
	"sap/ui/core/Control"
	], function(Control){
		"use strict";
		return Control.extend( "CustomControl.CustomControl.Control.ProductRating", {
			metadata: {
			},
			init : function( ){},
			renderer : function( oRm, oControl ) {}
		}); 
	} );